{
  "name": "pocitatko",
  "private": true,
  "version": "0.4.2",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check-generated.mjs",
    "test:rules": "npx --yes firebase-tools@15.26.0 emulators:exec --only firestore \"node --test tests/firestore-rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "esbuild": "^0.25.8"
  },
  "dependencies": {
    "firebase": "^12.16.0"
  }
}
